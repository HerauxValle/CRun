/*
 * tests/hello.go
 * Minimal Go test — single file, package main.
 * Run with: crun tests/hello.go
 * Note: Go requires the file to declare package main and have a main() func.
 */
package main

import "fmt"

func main() {
	fmt.Println("crun: Go compilation OK")
}
